name: Dispatch CI

on:
  workflow_dispatch:

jobs:
  linux-64_pixi:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false

    steps:
      - name: Checkout pull request branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run setup script
        run: |
          sed -i "s/main/${GITHUB_REF}/g" install.sh
          sed -i "s/main/${GITHUB_REF}/g" pixi_setup.sh
          ./pixi_setup.sh

  osx-64_pixi:
    runs-on: macos-latest
    strategy:
      fail-fast: false

    steps:
      - name: Checkout pull request branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run setup script
        run: |
          sed -i "s/main/${GITHUB_REF}/g" install.sh
          sed -i "s/main/${GITHUB_REF}/g" pixi_setup.sh
          ./pixi_setup.sh

  osx-arm64_pixi:
    runs-on: macos-14
    strategy:
      fail-fast: false

    steps:
      - name: Checkout pull request branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run setup script
        run: |
          sed -i "s/main/${GITHUB_REF}/g" install.sh
          sed -i "s/main/${GITHUB_REF}/g" pixi_setup.sh
          ./pixi_setup.sh
